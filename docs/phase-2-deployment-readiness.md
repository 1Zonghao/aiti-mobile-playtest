# Phase 2 部署准备度

## 当前配置

- 投票环境变量：`NEXT_PUBLIC_VOTE_URL`
- 当前仓库/验证环境：未配置
- 未配置或非 HTTP(S) 时：按钮禁用并显示“投票入口即将开放”
- 配置后：先显示外链提示，用户确认后才以新标签打开

## 数据与网络

- 核心页面由 Next.js 静态生成，不依赖服务端数据。
- 无真实大模型调用、远程字体、账户、数据库、排行榜、社区或身份采集。
- 测试状态与匿名统计只在本地存储；统计可在 `/about` 关闭。
- 正式角色图尚未接入，因此当前首页没有大图资源，初始资源目标风险较低。

## 验证清单

- `npm run lint`: 通过
- `npm run typecheck`: 通过
- `npm run test`: 通过，30项
- `npm run build`: 通过，29个页面（含16个类型详情）
- `npm run test:e2e`: 通过，4条浏览器流程
- 360px 无横向滚动：浏览器断言通过
- reduced motion：浏览器媒体模拟通过
- 生产 `/debug`：`next start` 黑盒确认404；同一服务根路径返回200

## 上会前必须补齐

1. 接入并压缩正式 WebP 角色资产，至少优先首发8型。
2. 配置并人工点击验证真实 `NEXT_PUBLIC_VOTE_URL`。
3. 补充大会名称、展位、海报/论文与作者信息。
4. 在目标手机和大会网络进行实体扫码、长页截图、弱网与对比度人工检查。
5. 对问题文案与哄感公式完成最终人工批准；当前仍为草案状态。
